# G1-01 A 执行提示词

将下列完整文本发给实施模型；详细范围与验收以 [任务书](g1_01_handoff.md) 为准。本文件的存在不授予执行权限，由用户转发并明确要求实施后开始。

```text
请执行 /home/lcq/agent_workload_characterization/docs/g1_01_handoff.md 的 A 阶段。

先完整阅读项目四份必读文档，再读任务书要求的契约、现有实现和 RUN-01-C-v2 交付。不要重复已验收的 RUN-01 或重新设计框架。

本次授权范围仅为：
1. 固定 RUN-01 C 原始数据只读分析，恢复 26 次工具调用身份与时间证据；缺失时间保持未知，不编造独占 Tool CPU。
2. 在现有 mini 执行链接入未来工具事件，补宿主 mini/collector 的最小 CPU/RSS 观测；历史宿主缺口不回填。
3. 执行任务书明确限额内的离线 fixtures、真实 mini + 封网 fake transport/执行器测试，以及本地自有确定性小程序计量/开销核对。
4. 在全新目录交付派生报告、完整性清单、G1 逐项证据及 B 容器验证审批提案。

先列简短实施计划与本地合成用例/预算，再在 A 范围内合并完成。不逐文件询问；遇到范围外权限停止申请。

禁止安装/下载/联网、读取真实密钥、启动或操作容器/镜像、执行 trace 命令或 candidate、第二次真实模型任务、perf/eBPF/提权、改全局设置、改 references/旧源/封存数据、覆盖历史报告、提交或推送 Git。

B 只写审批清单，不执行；不自行宣布 G1 通过。不为缺失数据自动重采，不为样本量或表格齐全扩展任务。

交付时列实际改动文件、实际命令与结果、scope/时间/资源覆盖、缺失与噪声、输入输出哈希、未执行事项和 B 待批准范围。失败结果原样保留。

A 完成后停止，交回原评审核验。不要继续 B 或其他任务。
```
