# G1-01 B 执行提示词

先将下面第一段发给实施模型。详细范围以 [B 任务书](g1_01_b_handoff.md) 为准；当前只放行 B0，不放行容器操作。

## 第一段：现在可转发，离线准备

```text
请按 /home/lcq/agent_workload_characterization/docs/g1_01_b_handoff.md 完成 B0：无模型容器验证入口的离线组装与最终审批清单。

先完整阅读任务书及必读文档。G1-01 A 已验收，不重做 A、不重跑历史机制实验，不增加 benchmark/模型/归因框架。

复用已有 ToolEventRecordingEnvironment、AttachedDockerEnvironment、资源采样、宿主观测和停止链。允许将内嵌 hook 最小抽取为共享实现，原 mini 路径与 B 必须使用同一实现；不能复制 hook 或启动 Agent loop 来获取事件。

实现默认无副作用计划模式、双旗标执行门禁、准确的两个顺序容器用例、预算/停止/归档/清理接线，完成任务书八节的最低离线回归。记录真实可运行命令、解释器、代码/配置/脚本 SHA、两容器完整用例清单、180s 总预算及清理预留、磁盘与 I/O 降级。

本次只允许自有代码/测试/新文档与离线 fake 验证。禁止 Docker/镜像操作、模型/网络、安装/下载、读取密钥、运行任务/gold/candidate、改 references/旧源/历史报告、提交或推送 Git。

在新交付中更正历史说明：R3 manifest 有 files map，只是 summary.md 哈希过期。不要覆盖 R3/R4 原件。

B0 合并完成后交付实际改动、测试、确切命令和最终审批清单，标 B_user_approval=pending，停止。不要自行进入 B1；用户转发本提示词不构成容器执行批准。
```

## 第二段：B0 清单验收且用户明确批准后才能使用

以下不是当前授权。实施模型不得自行填入批准记录或把此模板当作批准。

```text
请仅执行我本次明确批准的 G1-01 B1 最终清单（以我指定的清单路径、代码/配置身份与批准记录为准）。先复核身份与预算；缺批准或有漂移即停止。

本批最多两个顺序的固定 digest arm64 容器，network none，各≤2 CPU/256 MiB，总 wall≤180s，使用已有本地镜像；无模型、无 Agent/benchmark/gold/verifier 任务。只执行任务书和已批准清单中的合成用例，不 pull/build、不读取凭据、不扩预算。

一次执行即停，意外失败不重试；执行正确的停止、身份限定清理和最小失败证据归档。保留原始计数、工具事件、宿主身份与快照、缺失/降级、停止分类、清理结果及完整哈希。不得把共享 scope CPU 当独占 Tool CPU。

完成后交付新报告包与 delivery，交回原评审核验。不宣布 G1 自动通过，不启动第二批或真实模型任务。
```
